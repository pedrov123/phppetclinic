{% extends '::base.html.twig' %}

{% block title -%}PetType{% endblock %}

{% block body -%}
    <h1>PetType list</h1>

    <table class="records_list table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('pettype_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('pettype_show', { 'id': entity.id }) }}">
                            <span class="glyphicon glyphicon-arrow-right" aria-hidden="true"></span> show</a>
                    </li>
                    <li>
                        <a href="{{ path('pettype_edit', { 'id': entity.id }) }}">
                            <span class="glyphicon glyphicon-edit" aria-hidden="true"></span> edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('pettype_new') }}">
                <span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span> Create a new PetType
            </a>
        </li>
    </ul>
    {% endblock %}
