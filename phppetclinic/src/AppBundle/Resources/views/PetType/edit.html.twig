{% extends '::base.html.twig' %}

{% block title -%}PetType{% endblock %}

{% block body -%}
    <h1>PetType edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('pettype') }}">
            <span class="glyphicon glyphicon-list" aria-hidden="true"></span> Back to the PetType list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
