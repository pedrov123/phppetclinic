{% extends '::base.html.twig' %}

{% block title -%}Vet{% endblock %}

{% block body -%}
    <h1>Vet creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('vet') }}">
            <span class="glyphicon glyphicon-list" aria-hidden="true"></span> Back to the Vet list
        </a>
    </li>
</ul>
{% endblock %}
