{% extends '::base.html.twig' %}

{% block title -%}Visit{% endblock %}

{% block body -%}
    <h1>Visit creation</h1>

    {{ form(form) }}

    <ul class="record_actions table table-striped">
        <li>
            <a href="{{ path('owner_show',{'id': entity.pet.owner.id }) }}">
                <span class="glyphicon glyphicon-backward" aria-hidden="true"></span> Back to the Owner
            </a>
        </li>
    </ul>
{% endblock %}